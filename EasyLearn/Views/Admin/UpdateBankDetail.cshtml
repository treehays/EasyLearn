@model EasyLearn.Models.DTOs.PaymentDetailDTOs.PaymentDetailDTO

@{
    //ViewBag.Title = "title";
    Layout = "_Layout";
}

<h2>title</h2>

<br>
<div class="row row-content d-flex justify-content-center">
    <div class="col-md-6">
        <div class="card">
            <h3 class="card-header bg-black text-center"><strong>Update Admin Bank Details</strong></h3>
            <div class="card-body">
                <form method="POST">
                    <p>Check the details carefully before clicking on the Update button</p>
                    <input name="Id" value="@Model.Id" type="hidden" />
                    <div>
                        @Html.DisplayNameFor(x => x.BankName)
                        <input name="BankName" value="@Model.BankName" type="text" class="form-control" required />
                    </div>
                    <div>
                        <label asp-for="AccountNumber" class="control-label"></label>
                        <input name="AccountNumber" value="@Model.AccountNumber" type="text" class="form-control" required />
                    </div>
                    <div>
                        <label asp-for="AccountName" class="control-label"></label>
                        <input name="AccountName" value="@Model.AccountName" type="text" class="form-control" required />
                    </div>

                    <div>
                        <label asp-for="AccountType" class="control-label"></label>
                        <input name="AccountType" value="@Model.AccountType" type="text" class="form-control" required />
                    </div>


                    <br />
                    <button type="submit">Update</button>

                </form>
            </div>
        </div>
    </div>
</div>